import { CompanionIncome } from '../types'

export const DEFAULT_CHILDREN_ABOVE_SIX = 0
export const DEFAULT_CHILDREN_BELOW_SIX = 0
export const DEFAULT_SEVERELY_DISABLED = false
export const DEFAULT_COMPANION_INCOME: CompanionIncome = {
    applied: false,
    income: undefined,
}
